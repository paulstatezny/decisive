'use strict';

var React = require('react');

module.exports = React.createClass({
    displayName : 'DecisiveApplication',

    render : function()
    {
        return (
            <div>Decisive</div>
        );
    }
});
